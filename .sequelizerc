const path = require('path');
const env = process.env.NODE_ENV || 'development';

// Carrega o arquivo config.js que agora tem as configurações por ambiente
// O caminho é relativo à raiz do projeto onde .sequelizerc está.
const dbConfig = require(path.resolve(__dirname, 'src', 'config', 'config.js'))[env];

module.exports = {
  'config': dbConfig, // Passa o objeto de configuração específico do ambiente
  'models-path': path.resolve(__dirname, 'src', 'models'),
  'migrations-path': path.resolve(__dirname, 'src', 'migrations'),
  'seeders-path': path.resolve(__dirname, 'src', 'seeders')
};